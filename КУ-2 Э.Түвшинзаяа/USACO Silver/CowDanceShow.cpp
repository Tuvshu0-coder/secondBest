#include <bits/stdc++.h>
using namespace std;
#define int long long

void solve(){
    int N, maxTime;
    vector<int> time(N);
    for(int &t : time) cin >> t;
    int left = 1;
    int right = N;



}

signed main(){
    // int T;
    // cin >> T;
    // while(T--)
    solve();
}