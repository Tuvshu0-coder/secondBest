#include <windows.h>
#include <iostream>
#include <string>
#include <unordered_map>
using namespace std;

int main() {
    SetConsoleOutputCP(CP_UTF8);
    SetConsoleCP(CP_UTF8);
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);




}
